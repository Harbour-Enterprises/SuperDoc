// @ts-check

/**
 * Attribute handlers for <w:r>.
 * Currently, <w:r> itself has no direct attributes we map,
 * but this file exists to align with the v3 pattern
 * and to make future additions straightforward.
 *
 * @type {import('@translator').AttrConfig[]}
 */
export default [];
