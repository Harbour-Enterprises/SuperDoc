{
  "name": "@superdoc-dev/schema-management",
  "version": "0.0.1",
  "private": true,
  "description": "Schema freezing utilities and sidecar assets for @harbour-enterprises/super-editor",
  "type": "module",
  "scripts": {
    "freeze": "node --import tsx ./src/freeze-schema.ts",
    "check": "node --import tsx ./src/check-schemas.ts",
    "remove": "node --import tsx ./src/remove-schema.ts",
    "test": "vitest"
  },
  "files": [
    "src",
    "versions",
    "README.md"
  ],
  "engines": {
    "node": ">=18"
  },
  "dependencies": {},
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.0.4",
    "tsx": "^4.19.2",
    "vitest": "^3.0.8",
    "vite": "^6.3.5"
  }
}
